(configuration-layer/declare-layers
 '(systemd
   python
   helm
   auto-completion
   better-defaults
   emacs-lisp
   git
   ;; markdown
   multiple-cursors
   ;; org
   ;; (shell :variables
   ;;        shell-default-height 30
   ;;        shell-default-position 'bottom)
   ;; spell-checking
   syntax-checking
   version-control
   treemacs
   ))
